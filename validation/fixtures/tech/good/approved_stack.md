
# Approved Tech Stack Example

This document uses approved technologies:

```typescript
// TypeScript is approved
import { drizzle } from 'drizzle-orm/nile-serverless';

const db = drizzle(process.env.DATABASE_URL);
```

```sql
-- PostgreSQL is approved
SELECT * FROM tenants WHERE id = 'tenant-123';
```

```javascript
// JavaScript/Next.js is approved
export default function Page() {
  return <div>Hello World</div>;
}
```

All code blocks use approved languages and mention approved technologies.
